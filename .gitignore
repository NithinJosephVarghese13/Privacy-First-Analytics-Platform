# React
frontend/node_modules/
frontend/dist/
frontend/.env

# .NET
**/bin/
**/obj/

# IDEs
.vs/
.vscode/
.idea/

# Logs
*.log

.env

# Docker secrets for identity hashing (FR-2.1). The signing key and daily-salt seed must NEVER be
# committed — they are generated per-environment by docker/secrets/generate-secrets.sh.
docker/secrets/analytics_daily_salt_seed
docker/secrets/analytics_durable_hmac_key